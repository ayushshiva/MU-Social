- [ ] Root-cause analysis (navigation + UI + ViewModel + repository + Firestore + models + DI + auth)
- [ ] Add production logs to ChatListScreen/ChatListViewModel/ChatRepositoryImpl
- [ ] Fix ChatListScreen so it doesn’t block/overlay LazyColumn with loading/error incorrectly and avoids per-row collecting
- [ ] Fix ViewModel state management to use StateFlow and collect in Compose with lifecycle
- [ ] Fix repository/Firestore deserialization ordering + default fields safety
- [ ] Validate result via local build/test commands and/or runtime checks

